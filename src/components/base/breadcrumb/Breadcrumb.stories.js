import Breadcrumb from "./Breadcrumb";
import Skeleton from "../skeleton";
import { useEffect, useState } from "react";

export default {
  title: "Breadcrumb",
};

/**
 * Returns all primary buttons (Default button style)
 *
 */
export function Breadcrumbb() {
  const type = "primary";

  return <div>Breadcrumb..</div>;
}
